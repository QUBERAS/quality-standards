# Level: standard
# Bugs + security + common footguns. Recommended for active repos.

line-length = 120
target-version = "py312"
extend-exclude = ["*.ipynb"]

[lint]
select = [
    "F",   # pyflakes
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "B",   # bugbear — likely bugs
    "S",   # bandit — security
    "I",   # isort
    "UP",  # pyupgrade
]
ignore = [
    "E501",  # line too long (handled by formatter)
    "E741",  # ambiguous variable name (false positives on short math/science vars)
    "S106",  # hardcoded-password-func-arg (false positives)
    "S108",  # hardcoded-temp-file
    "S311",  # non-cryptographic random
]

[lint.per-file-ignores]
"**/tests/**" = ["S101", "F811", "B017", "B023"]
"**/test_*.py" = ["S101", "F811", "B017", "B023"]
"**/*_test.py" = ["S101", "F811", "B017", "B023"]
"**/conftest.py" = ["S101", "F811"]
"**/alembic/versions/*" = ["F401", "UP"]

[format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
